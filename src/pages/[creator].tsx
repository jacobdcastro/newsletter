/*
 * @route    /{username}
 * @desc     Page where users go to see content from a specific creator, or subscribe if not already
 * @access   Public
 * @wallet   Required for content, not required to see profile
 */
import React from 'react';

interface Props {}

export const CreatorPage = (props: Props) => {
  return <div></div>;
};
